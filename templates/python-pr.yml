name: Develop CI

run-name: PR CI

on:
  pull_request:
    types: [opened, synchronize, closed]

jobs:
  ci:
    uses: PeterChauYEG/labone-actions/.github/workflows/python-pr.yml@main
    secrets: inherit
    # with:
    #   lint_path: src   # defaults to '.' (whole repo)
    #   Turn off any job your repo doesn't need, e.g.:
    #   enable_test: false
    #   enable_security_scan: false   # if a sibling security.yml already covers Trivy
    #   enable_dependency_audit: true # off by default - opt in explicitly (pip-audit)
    #   Also fold in github-actions[bot] here if this repo's main.yml
    #   pre-creates check-runs for its own CalVer version-bump PR:
    #   also_treat_actions_bot_as_dependabot: true
