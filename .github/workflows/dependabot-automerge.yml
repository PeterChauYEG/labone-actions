name: Dependabot Auto-merge

# Dependabot is disabled. This file is kept as a stub so callers that
# reference this reusable workflow don't break.

on:
  workflow_call: {}

jobs:
  automerge:
    name: Auto-merge minor/patch updates (disabled)
    runs-on: [self-hosted, catfood]
    if: false
    steps:
      - run: echo "Dependabot auto-merge is disabled"
